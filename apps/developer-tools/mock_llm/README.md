# Mock LLM

This mock server is OpenAI-compatible. Connect to it from the Rhesis UI using the **LiteLLM Proxy** provider.

## Start

```bash
./start.sh
```

The server listens on `http://localhost:18080`.
