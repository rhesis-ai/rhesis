# ExternalSecret: syncs GitHub App credentials from GCP Secret Manager into a
# Kubernetes Secret consumed by the ARC runner scale set.
#
# Prerequisites (one-time, after Terraform apply creates the GSM secrets):
#   gcloud secrets versions add prd-arc-github-app-id \
#     --project=rhesis-prd --data-file=- <<< "<APP_ID>"
#   gcloud secrets versions add prd-arc-github-app-installation-id \
#     --project=rhesis-prd --data-file=- <<< "<INSTALLATION_ID>"
#   gcloud secrets versions add prd-arc-github-app-private-key \
#     --project=rhesis-prd --data-file=/path/to/private-key.pem
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: arc-github-app-secret
  namespace: arc-runners
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: arc-github-app-secret
    creationPolicy: Owner
  data:
    - secretKey: github_app_id
      remoteRef:
        key: prd-arc-github-app-id
    - secretKey: github_app_installation_id
      remoteRef:
        key: prd-arc-github-app-installation-id
    - secretKey: github_app_private_key
      remoteRef:
        key: prd-arc-github-app-private-key
