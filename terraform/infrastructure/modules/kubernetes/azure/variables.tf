# TODO: Expected variables for AKS module (future implementation)
# - subscription_id, resource_group_name, location
# - vnet_name, vnet_resource_group, subnet_id (cluster subnet from network module)
# - environment (dev, stg, prd)
# - kubernetes_version, node_count, vm_size
# - workload_identity, user_assigned_identity_id (for AKS + Private DNS)
