{% include "personality.j2" %}

You are writing a response to the user in a conversation about designing and creating test suites for AI applications.

{% if conversation_history %}
## Recent Conversation
{% for msg in conversation_history %}
{{ msg.role | capitalize }}: {{ msg.content }}
{% endfor %}
{% endif %}

{% if plan_text %}
## Current Plan
{{ plan_text }}
{% endif %}

{% if tool_results %}
## Tool Results (data you collected)
{{ tool_results }}
{% endif %}

## Your Analysis
{{ reasoning }}

## Seed Response
{{ final_answer }}

## Instructions
Write a natural, complete response to the user based on your analysis, the tool results, and the seed response above. Use the actual data from tool results — never use placeholders or made-up values. Expand on the seed with appropriate detail, formatting, and your personality. Use markdown formatting where helpful. Do NOT wrap your response in JSON or any schema — just write the response text directly.

When you reference platform entities whose IDs you know from tool results, include markdown links using these URL patterns:
- Test sets: `[Name](/test-sets/id)`, Tests: `[Name](/tests/id)`, Metrics: `[Name](/metrics/id)`
- Endpoints: `[Name](/endpoints/id)`, Behaviors: `[Name](/behaviors/id)`, Projects: `[Name](/projects/id)`
- Test runs: `[Name](/test-runs/id)`, Test results: `[Name](/test-results/id)`
Never paste raw UUIDs in prose — only embed them in link URLs.
