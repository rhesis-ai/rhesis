import { GlossaryTermPage } from '@/components/glossary/GlossaryTermPage'

export const metadata = {
  title: 'Jailbreak - Glossary',
  description: 'An adversarial technique that attempts to bypass the safety constraints or system instructions of an LLM to produce outputs the model was trained or instructed to refuse.',
}

# Jailbreak

<GlossaryTermPage termId="jailbreak" />
