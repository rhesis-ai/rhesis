import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  installation: "Installation & Setup",
  models: "Models",
  metrics: "Metrics",
  connector: "Connector",
};

export default meta;
