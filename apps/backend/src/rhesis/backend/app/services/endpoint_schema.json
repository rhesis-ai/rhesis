{
    "input_schema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "User query" },
        "conversation_id": { "type": "string", "description": "Conversation identifier" }
      },
      "required": ["input"]
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "output": { "type": "string", "description": "Extracted response message" },
        "metadata": { "type": "object", "description": "Metadata about the response" },
        "context": { "type": "array", "description": "Context used for the response" },
        "version_info": { "type": "object", "description": "Version of the system under test for this response (prompt version, model, parameters)" }
      }
    }
  }