name: "[Release] Deploy all to staging"

on:
  push:
    branches: release/*

jobs:
  deploy-all-to-staging:
    uses: ./.github/workflows/deploy-all-k8s.yml
    with:
      environment: stg
    secrets: inherit
