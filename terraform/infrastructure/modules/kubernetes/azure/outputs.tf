# TODO: Expected outputs for AKS module (future implementation)
# - cluster_id, cluster_name, cluster_fqdn (private)
# - kube_config (or kube_config_raw), cluster_identity
# - private_fqdn, private_dns_zone_id
