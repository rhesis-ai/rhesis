# Vertex AI Deployment Configuration
# Copy this file to .env and fill in your values

# GCP Project Configuration (Required)
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
GCP_SERVICE_ACCOUNT=your-service-account@your-project.iam.gserviceaccount.com

# Model path and default model (use same as GitHub secrets)
# POLYPHEMUS_MODEL_PATH = GCS parent path to the model folder (not including the model folder name)
# POLYPHEMUS_MODEL_BUCKET = GCS bucket name (optional; derived from path if not set)
# POLYPHEMUS_DEFAULT_MODEL = Model folder name under POLYPHEMUS_MODEL_PATH
# Full path = POLYPHEMUS_MODEL_PATH/POLYPHEMUS_DEFAULT_MODEL (e.g. gs://your-bucket-name/cache/your-model-name)
POLYPHEMUS_MODEL_PATH=gs://your-bucket-name/cache
POLYPHEMUS_DEFAULT_MODEL=your-model-name

# HuggingFace Token (Optional)
# Required only for private HuggingFace models
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Optional: Path to service account key file (if not using default credentials)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
