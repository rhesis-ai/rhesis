import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Overview",
  polyphemus: "Polyphemus",
  "requesting-access": "Requesting Access",
  "sdk-usage": "Using Polyphemus with the SDK",
};

export default meta;
