import { GlossaryTermPage } from '@/components/glossary/GlossaryTermPage'

export const metadata = {
  title: 'Test Set Type - Glossary',
  description: 'A required classification assigned to a test set that determines which kind of tests it can contain and how it is executed—either single-turn or multi-turn.',
}

# Test Set Type

<GlossaryTermPage termId="test-set-type" />
