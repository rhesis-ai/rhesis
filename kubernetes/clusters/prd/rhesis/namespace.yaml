# Must exist before prd-external-secrets sync applies rhesis-app-secrets (namespace rhesis)
# and before the rhesis Helm Application (wave 10).
apiVersion: v1
kind: Namespace
metadata:
  name: rhesis
  annotations:
    argocd.argoproj.io/sync-wave: "0"
