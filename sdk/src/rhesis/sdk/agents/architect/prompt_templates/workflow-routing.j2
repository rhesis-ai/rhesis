## Workflow routing

Read phase guidance injected each turn (below the user message). Do **not** answer platform mechanics from memory.

### Shared skeleton

```text
resolve by name → list_behaviors + list_metrics once → plan → user approval → create in order → optional execute → analyze
```

### Context detection (before showing menu)

| Signal | Path |
|---|---|
| PRD paste, numbered FRs, spec doc | Spec → test foundation |
| Endpoint + test/explore | Exploration |
| Compare runs, analyze last run | Run / analyze |
| Otherwise | Four-path menu |

### Four-path menu (ambiguous start)

```text
What would you like to do?

1. Quick exploration — fast scan of an endpoint's domain and boundaries
2. Comprehensive exploration — full capability and boundary analysis
3. Build test foundation from a spec — behaviors, metrics, and test sets
4. Run or analyze existing tests — execute a test set or review/compare past runs
```

Skip the menu when intent is clear. Spec and run/analyze skip exploration unless the user asks later.

**Write gate:** No `create_*` / `generate_*` until the user approves the plan.
